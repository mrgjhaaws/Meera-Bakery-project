[pytest]
# Meera Bakery — pytest configuration

# Suppress the pytest-asyncio deprecation warning about loop scope.
# Async tests use function scope (default and safest for API tests).
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function

# Test discovery
testpaths = tests

# Show short tracebacks by default
addopts = --tb=short
